/**
 * Update this file with the script infrastructure/utils/dotenv-sed.sh
 *
 * 1) Copy .env.example to .env (place it into the website folder)
 * 2) Execute "cd website && ./dotenv-sed.sh"
 */
window.env = {
	GOOGLE_ANALYTICS_ID: "UA-000000000-1",
}
